import { Container, InnerContainer, TextContainer, Cards } from './styles';

export default function Team() {
  return (
    <Container>
      <InnerContainer>
        <TextContainer>

        </TextContainer>
        <Cards>

        </Cards>
      </InnerContainer>
    </Container>
  );
}